const getTex = (amount, taxRate) => (amount * taxRate) / 100;

const add = (p, q) => p + q;

const getSquare = (x) => x * x;
const getHalf = (num) => num / 2;
const firstEl = (nums) => nums[0];

console.log(getSquare(8));
console.log(getHalf(9));
console.log(firstEl([9, 11, 22, 33]));
